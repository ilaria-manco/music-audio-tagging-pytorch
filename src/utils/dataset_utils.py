# import numpy as np
# import config_file


# def rename_files(file_path):
#     # for i in file_path:
#         # if "musicradar"


# def put_files_in_class_dir():
    
    
# def extract_tags(text_file):
#     """ Extract tag text from text file """
#     textfile_path = config_file.DATA_PATH + text_file
#     tags = np.zeros(len(text_file))
#     for text, index in enumerate(np.loadtxt):
#         # TODO: add some regex or string stripping to get tag
#         tag = ""
#         tags[index] = tag
#     return tags

# def id_to_path(index_file):
